name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest' # macOS (Apple Silicon + Intel)
            args: '--target universal-apple-darwin'
          - platform: 'ubuntu-22.04' # Linux
            args: ''
          - platform: 'windows-latest' # Windows
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 全履歴を取得（リリースノート生成に必要）

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          # Those targets are only used on macOS runners so it's in an `if` to slightly speed up windows and linux builds.
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm install

      - name: Build the app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: '箱書きエディタ ${{ github.ref_name }}'
          releaseBody: 'ビルド中です。完了後、リリースノートが自動生成されます。'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

  update-release-notes:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate Release Notes
        id: generate_notes
        uses: actions/github-script@v7
        with:
          script: |
            const tagName = context.ref.replace('refs/tags/', '');
            
            // getReleaseByTag doesn't support draft releases, so we search in listReleases
            const releases = await github.rest.repos.listReleases({
              owner: context.repo.owner,
              repo: context.repo.repo
            });
            
            const release = releases.data.find(r => r.tag_name === tagName);
            
            if (!release) {
              throw new Error(`Release not found for tag ${tagName}`);
            }

            const { data: notes } = await github.rest.repos.generateReleaseNotes({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: context.ref.replace('refs/tags/', ''),
              previous_tag_name: undefined
            });

            const version = context.ref.replace('refs/tags/', '').replace('v', '');
            const releaseBody = [
              `# 箱書きエディタ ${context.ref.replace('refs/tags/', '')}`,
              '',
              '## 📦 ダウンロード',
              '',
              '各プラットフォーム用のインストーラーは下記の Assets からダウンロードできます。',
              '',
              '### macOS',
              `- \`箱書きエディタ_${version}_universal.dmg\``,
              '',
              '### Windows',
              `- \`箱書きエディタ_${version}_x64-setup.exe\``,
              `- \`箱書きエディタ_${version}_x64_en-US.msi\``,
              '',
              '### Linux',
              `- \`箱書きエディタ_${version}_amd64.deb\``,
              `- \`箱書きエディタ_${version}_amd64.AppImage\``,
              '',
              '---',
              '',
              notes.body
            ].join('\n');

            await github.rest.repos.updateRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: release.id,
              body: releaseBody
            });

  post-to-x:
    needs: update-release-notes
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      post-to-x:
    needs: update-release-notes
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Post to X (Twitter)
        uses: ethomson/send-tweet-action@v1
        with:
          # ここを env ではなく with の中に記述します
          consumer-key: ${{ secrets.TWITTER_CONSUMER_API_KEY }}
          consumer-secret: ${{ secrets.TWITTER_CONSUMER_API_SECRET }}
          access-token: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          access-token-secret: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
          status: |
            📦 箱書きエディタ ${{ github.ref_name }} をリリースしました！

            ダウンロード: https://github.com/GennosukeSatsuki/hakogaki/releases/tag/${{ github.ref_name }}

            #箱書きエディタ #小説執筆 #創作支援